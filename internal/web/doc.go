// Package web serves the control panel: live view, camera status, event
// listings, and config editing with hot-reload. Mirrors v1's
// control_panel.py as a small embedded HTTP server.
package web
